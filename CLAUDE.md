@AGENTS.md

## 必須ルール
- Session開始時にはメモリーを確認してから回答する
- **コードを1文字でも変更・削除・移動する前に、必ず変更方針（対象ファイル、変更内容、削除/移動の有無）を提示し、ユーザーの明示的なOKを得ること。指示が明確・単純に見える場合も例外なく行う。ファイル移動やリファクタのみの作業も「実装」に含まれる**
- **ブラウザでの現状確認（画面表示の確認、動作検証など）も、実行する前に何を確認するか提示し、ユーザーのOKを得てから行うこと。「確認のためだから許可不要」という例外は無い**
  - ただし Read/Grep によるファイル内容の調査は確認不要
- **議論の途中で実装しない。原因調査や設計方針についてユーザーと会話が続いている間（質問されている、指摘・訂正されている等）は、まだGOサインではない。「ok」「進めて」「やって」など明確な肯定の一言をもらうまでは、Edit/Write/Bash（変更を伴うもの）を一切使わない**

### コードスタイル (Prettier 互換)
- 2スペースインデント、セミコロンなし
- シングルクォート（JS/JSX）
- 100文字行長、末尾カンマ必須
- 未使用変数は `_` プレフィックスでOK

### CSS（このプロジェクト固有 / Tailwind は不使用）
- スタイルは `styles/`（SCSS）と各コンポーネントの CSS Modules（`*.module.scss`）で記述
- ルートで `@/styles/global.scss` を読み込む（app/layout.tsx）
- 条件付きクラスには `clsx` を使用
- タグ指定styleは使用しない。必ずclass指定styleにすること。
  - ただし `styles/global.scss` 等のリセット/共通スタイルは対象外
